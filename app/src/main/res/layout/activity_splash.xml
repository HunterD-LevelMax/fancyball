<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/splashRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_splash">

    <View
        android:layout_width="360dp"
        android:layout_height="360dp"
        android:layout_gravity="top|start"
        android:layout_marginStart="-150dp"
        android:layout_marginTop="42dp"
        android:alpha="0.9"
        android:background="@drawable/bg_splash_glow_blue" />

    <View
        android:layout_width="340dp"
        android:layout_height="340dp"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="-110dp"
        android:layout_marginBottom="42dp"
        android:alpha="0.86"
        android:background="@drawable/bg_splash_glow_gold" />

    <View
        android:layout_width="match_parent"
        android:layout_height="140dp"
        android:layout_gravity="center"
        android:alpha="0.52"
        android:background="@drawable/bg_splash_ribbon" />

    <LinearLayout
        android:id="@+id/splashContent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:orientation="vertical"
        android:paddingStart="32dp"
        android:paddingEnd="32dp">

        <FrameLayout
            android:id="@+id/logoContainer"
            android:layout_width="116dp"
            android:layout_height="116dp"
            android:layout_marginBottom="22dp"
            android:background="@drawable/bg_splash_logo"
            android:elevation="18dp"
            android:padding="18dp">

            <ImageView
                android:id="@+id/splashLogo"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:contentDescription="@string/app_name"
                android:scaleType="fitCenter"
                android:src="@drawable/icon" />
        </FrameLayout>

        <TextView
            android:id="@+id/splashTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:includeFontPadding="false"
            android:text="@string/loading_title"
            android:textColor="@color/fb_text_primary"
            android:textSize="30sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/splashTagline"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:gravity="center"
            android:includeFontPadding="false"
            android:letterSpacing="0.12"
            android:text="@string/loading_tagline"
            android:textColor="@color/fb_gold_light"
            android:textSize="11sp"
            android:textStyle="bold" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="28dp"
            android:background="@drawable/bg_splash_panel"
            android:orientation="vertical"
            android:padding="16dp">

            <ProgressBar
                android:id="@+id/splashProgress"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="match_parent"
                android:layout_height="14dp"
                android:indeterminate="false"
                android:max="1000"
                android:progress="0"
                android:progressDrawable="@drawable/progress_splash" />

            <TextView
                android:id="@+id/splashSubtitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:gravity="center"
                android:includeFontPadding="false"
                android:text="@string/loading_subtitle"
                android:textColor="@color/fb_text_secondary"
                android:textSize="12sp"
                android:textStyle="bold"
                android:visibility="gone" />
        </LinearLayout>
    </LinearLayout>
</FrameLayout>
